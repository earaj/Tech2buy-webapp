<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détail du produit</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="commun.css">
    <link href='https://fonts.googleapis.com/css?family=Barlow+Semi+Condensed' rel='stylesheet'>
</head>
<header>
    <%- include("../partials/headTech2buy.ejs") %>
</header>
<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6">
                <img src="<%= produit.image_url %>" height="750" width="750" class="img-fluid" alt="<%= produit.nom_produit %>">
            </div>
            <div class="col-md-6">
                <h1 class="product-name"><%= produit.nom_produit %></h1>
                <h2><%= produit.description_produit %></h2> <br><br>
                <h3><%= produit.prix_unitaire %> $</h3>
                <p><strong>Disponible:</strong> <%= produit.stock_disponible %> unités</p>
                <p><strong>Dimensions:</strong> <%= produit.dimensions %></p>
                <p><strong>Processeur (CPU):</strong> <%= produit.cpu %></p>
                <p><strong>Carte graphique (GPU):</strong> <%= produit.gpu %></p>
                <p><strong>Mémoire (RAM):</strong> <%= produit.ram %></p>
                <p><strong>Stockage:</strong> <%= produit.stockage %></p>
                <p><strong>Système d'exploitation:</strong> <%= produit.Systeme_dexploitation %></p>
                <p><strong>Taille d'écran:</strong> <%= produit.taille_ecran %> pouces</p>
                <form action="/ajouterAuPanier" method="post">
                    <input type="hidden" name="id_produit" value="<%= produit.id_produit %>">
                    <input type="hidden" name="image_url" value="<%= produit.image_url %>">
                    <input type="hidden" name="nom_produit" value="<%= produit.nom_produit %>">
                    <input type="hidden" name="description_produit" value="<%= produit.description_produit %>">
                    
                    <label for="quantity">Quantité:</label>
                    <input type="number" id="quantity" name="quantite" min="1" value="1" style="width: 7%;">

                    <input class="btn btn-primary btn-lg"  type="submit"
                    value="Ajouter au panier"></button>
                </form>
            </div>
        </div>
    </div>    
    <!-- Section de commentaires -->
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <h2 class="mb-4">Laisser un commentaire</h2>
            <form action="/submitComment" method="post">
                <input type="hidden" name="idProduit" value="<%= produit.id_produit %>">
                <div class="mb-3">
                    <textarea class="form-control" name="commentText" rows="3" placeholder="Votre commentaire..." required></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Publier le commentaire</button>
            </form>
        </div>
    </div>
</div>

<div class="container my-5">
    <h3>Commentaires</h3>
    <% commentaires.forEach(function(commentaire) { %>
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">Utilisateur <%= commentaire.id_utilisateur %></h5>
                <p class="card-text"><%= commentaire.commentaire %></p>
                <p class="card-text"><small class="text-muted">Posté le <%= commentaire.date_du_commentaire %></small></p>
            </div>
        </div>
    <% }); %>
</div>
    <script src="/js/bootstrap.min.js"></script>
</body>
</html>